"use client";

import WhatsappBotProView from "@/components/whatsapp-bot/WhatsappBotProView";

/** Panel vendedor — CRM premium + Evolution API (ver WhatsappBotProView). */
export default function WhatsappBotView() {
  return <WhatsappBotProView />;
}
